
@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        <li class="nav-item">
            <a class="nav-link text-dark hvr-grow" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Акаунт</a>
        </li>
        if (User.IsInRole(Common.GlobalConstants.AdministratorRoleName))
        {
            <div class="dropdown show">
                <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Admin
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" asp-area="Administration" asp-controller="Home" asp-action="Index">Администрация</a>
                </div>
            </div>
        }

        if (User.IsInRole(Common.GlobalConstants.DoctorRoleName))
        {
            <div class="dropdown show">
                <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Doctor
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" asp-area="DoctorRole" asp-controller="Doctor" asp-action="Edit">Промени профил</a>
                    <a class="dropdown-item" asp-area="DoctorRole" asp-controller="Appointment" asp-action="Index">Запазени часове</a>
                    <a class="dropdown-item" asp-area="" asp-controller="Chat" asp-action="All">Съобщения</a>
                </div>

            </div>
        }
        if (User.IsInRole(Common.GlobalConstants.PatientRoleName))
        {
            <div class="dropdown show">
                <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Patient
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" asp-area="PatientRole" asp-controller="Patient" asp-action="Edit">Промени профил</a>
                    <a class="dropdown-item" asp-area="PatientRole" asp-controller="Appointment" asp-action="Index">Запазени часове</a>
                    <a class="dropdown-item" asp-area="" asp-controller="Chat" asp-action="All">Съобщения</a>
                </div>
            </div>
        }
        <li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = string.Empty })">
                <button type="submit" class="nav-link btn btn-link text-dark">Излез</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark hvr-grow" asp-area="Identity" asp-page="/Account/Register">Регистрация</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark hvr-grow" asp-area="Identity" asp-page="/Account/Login">Влез</a>
        </li>
    }
</ul>
